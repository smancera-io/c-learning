/* Sentence to avoid generating diamong pattern or circular dependency, loading twice the same piece of code */
#pragma once
/* Own fuction store in a header file */
int sum (int a, int b) {
    return (a+b);
}